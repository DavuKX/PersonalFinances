apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: personal-finance
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: davukx/finance-analytics-service:1.0
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: analytics-service-config
            - secretRef:
                name: db-secrets
            - secretRef:
                name: rabbitmq-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
