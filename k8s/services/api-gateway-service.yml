apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: personal-finance
spec:
  type: ClusterIP
  selector:
    app: api-gateway-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
